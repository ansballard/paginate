{
  "name": "paginate",
  "version": "1.0.0",
  "description": "A simple dependency-free pagination module",
  "main": "paginate.js",
  "directories": {
    "test": "./node_modules/.bin/karma start Firefox --single-run"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aaron Ballard",
  "devDependencies": {
    "jasmine-core": "^2.3.4",
    "karma": "^0.12.37",
    "karma-firefox-launcher": "^0.1.6",
    "karma-jasmine": "^0.3.6"
  }
}
